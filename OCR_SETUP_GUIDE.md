# 腾讯云 OCR 开通与接入指南（拍照成表 · 云端高精度）

目标：开通腾讯云文字识别 OCR，拿到 SecretId / SecretKey，让「拍照成表」APP 的「云端高精度」识别可用，达到 95–99% 手写/印刷体准确率。

> 软件本身完全免费、开源、自托管。云 OCR 由**你自己的腾讯云账号**调用，产生的是云厂商费用（新账号有免费额度，默认不开通付费即零成本）。

## 一、整体流程（5 步）

1. 有腾讯云账号并完成**实名认证**（必须，否则无法调用 API）。
2. 进入文字识别控制台**开通服务**（即时生效）。
3. 在访问管理处**新建 API 密钥**（SecretId / SecretKey）。
4. **务必关闭后付费**（免费额度用完后自动停服而非扣费，最安全）。
5. 把密钥填进后端配置，本地验证。

## 二、分步操作

### 第 1 步：登录 / 注册腾讯云
- 打开 https://cloud.tencent.com ，用微信或手机号注册并登录。
- 进入「账号中心」完成**实名认证**（个人/企业均可，认证后才有 OCR 调用权限）。

### 第 2 步：开通文字识别服务
- 打开产品页 https://cloud.tencent.com/product/ocr ，点「立即使用」或「免费试用」。
- 进入控制台 https://console.cloud.tencent.com/ocr ，按提示开通「通用文字识别」相关接口。**开通即时生效**。
- 开通后，以下接口每月自动发放**免费额度（1000 次/月，当月有效）**：
  - 通用印刷体识别
  - 通用手写体识别
  - 表格识别（V2）
- 在「用量统计」页 https://console.cloud.tencent.com/ocr/stats 可随时查看剩余次数。

### 第 3 步：创建 API 密钥
- 打开 https://console.cloud.腾讯云.com/cam/capi （云 API 密钥管理）。
- 点「新建密钥」，生成一对 `SecretId` 和 `SecretKey`。
- **⚠️ SecretKey 仅显示一次**，务必立即复制保存。泄露后他人可盗用你的额度，如怀疑泄露可在同一页面「禁用/删除」。

### 第 4 步：关闭后付费（关键安全项）
- 进入文字识别控制台 → 「计费设置 / 费用」页（或 https://console.cloud.tencent.com/ocr ），确认**不要开通后付费（按量计费）**。
- 这样免费额度耗尽后调用会直接失败而不是扣费——对日常使用最安全。
- 如果未来用量确实很大，再按需购买「预付费资源包」（不从这里默认开启后付费）。

### 第 5 步：填入配置并验证
- 复制 `backend/.env.example` 为 `backend/config.json`，填入刚拿到的密钥：
  ```json
  {
    "secretId": "AKIDxxxxxxxx",
    "secretKey": "xxxxxxxx",
    "kdocsToken": "",
    "kdocsApiBase": "https://www.kdocs.cn"
  }
  ```
  （也可改用环境变量 `TENCENT_SECRET_ID` / `TENCENT_SECRET_KEY`）
- 启动后端并验证真实识别：
  ```bash
  cd photo-to-sheet-app
  npm install
  node backend/server.js
  ```
- 浏览器打开 `http://localhost:3000`，默认「云端高精度」模式，拍一张装配通知单测试。或在命令行快速自检：
  ```bash
  curl -s -X POST http://localhost:3000/api/recognize -H "Content-Type: application/json" -d "{\"image\":\"<base64>\",\"mode\":\"table\"}"
  ```
  返回不再是 DEMO 字样、而是真实表格数据即成功。

## 三、关于「可供几人同时使用」（容量说明）

| 环节 | 限制来源 | 说明 |
|------|----------|------|
| 前端页面托管 | 无硬上限 | 静态版/容器版都能承载大量并发访问 |
| 后端并发 | Node.js 单进程异步 | 普通服务器可支撑几十人同时在线请求 |
| 云端 OCR 频率 | 腾讯云 QPS | 默认有一定频控，可提工单提升；多人高频需评估 |
| **OCR 月度额度** | **1000 次/月（免费）** | **真正的硬约束**，超出需购买资源包 |

- 个人/小团队（≤10 人、每天偶尔拍）：1000 次/月免费额度足够，**零成本**。
- 车间/多人高频（每天几十张）：免费额度不够，需购买预付费资源包（单价约 ¥0.011/次起）。
- 当前已部署的纯静态公网版（仅本地识别）**不消耗 OCR 额度**，但手写编号识别差；要 96% 准确率必须走云端，才涉及上述额度。

## 四、你接下来要做的

1. 完成上面第 1–4 步（在浏览器操作）。
2. 把拿到的 `SecretId` / `SecretKey` 发给我（或自己填进 `backend/config.json`）。
3. 我可以帮你本地验证，或把后端连同密钥一起部署到公网容器，让任何手机任何网络都能用。

> 注：私钥属于敏感信息，建议在本地 `config.json` 填写，不要直接发到聊天里；若通过聊天发送，请注意仅在私密环境操作。
